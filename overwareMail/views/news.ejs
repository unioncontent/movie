<div class="main-body">
  <div class="page-wrapper">
    <div class="page-header">
      <div class="page-header-title">
        <h4>기사선택</h4>
      </div>
      <div class="page-header-breadcrumb">
        <ul class="breadcrumb-title">
          <li class="breadcrumb-item">
            <a href="/">
              <i class="fas fa-home"></i>
            </a>
          </li>
          <li class="breadcrumb-item"><a href="#!">기사선택</a></li>
        </ul>
      </div>
    </div>
    <div class="page-body">
      <div class="row" id="listTable">
        <div class="col-md-12">
          <div class="alert alert-default" style="border-radius:  0; border-width:  2px;  margin-bottom: 1rem;">
          	<ul class="p-l-15 f-12" style="list-style-type: disc; color:  #666;">
              <li>기사의 썸네일은 넣고자 하는 이미지의 URL 주소를 해당 기사의 박스 칸에 작성하시면 됩니다.</li>
              <li>썸네일, 대분류, 세부기사 수정 및 선택기사 삭제는 선택기사 리스트 페이지에서 할 수있습니다.</li>
              <li>대표기사에 있는 번호를 다른 기사의 세부기사 칸에 작성해주시면 기사 그룹핑이 됩니다.</li>
          	</ul>
        	</div>
        </div>
        <div class="col-md-12">
          <div class="card">
            <div class="card-header">
              <select name="select" class="col-md-1 form-control form-control-default m-b-10 m-r-5 p-r-5 f-left" id="selectKeyword">
                <option>키워드</option>
                <%for(var i=0; i < klist.length; i++) {%>
                  <option value='<%=klist[i].M_keyword%>'><%=klist[i].M_keyword%></option>
                <%}%>
              </select>
              <select class="form-control col-md-1 form-control-default m-r-5 m-b-10 f-left" id="selectNewsType">
                <option value="0">대분류</option>
                <option value="1">쇼박스영화</option>
                <option value="2">경쟁영화</option>
                <option value="3">캐스팅</option>
                <option value="4">쇼박스기업뉴스</option>
                <option value="5">영화일반</option>
                <option value="6">보도국</option>
                <option value="7">박스오피스</option>
              </select>
              <select class="form-control col-md-1 form-control-default m-r-5 m-b-10 f-left" id="selectNum">
                <option value="30">30</option>
                <option value="50">50</option>
                <option value="100">100</option>
              </select>
              <!-- <button class="btn btn-primary searchBtn">단독</button> -->
              <!-- <button class="btn btn-primary searchBtn">공식</button> -->
              <div class="col-sm-2 p-l-0 p-t-5 m-b-10 f-left border-checkbox-section">
                <div class="border-checkbox-group border-checkbox-group-default m-r-5">
                  <input class="border-checkbox" type="checkbox" id="searchBtn1" name="search" value="단독">
                  <label class="border-checkbox-label" for="searchBtn1">단독</label>
                </div>
                <div class="border-checkbox-group border-checkbox-group-default m-r-5" >
                  <input class="border-checkbox" type="checkbox" id="searchBtn2" name="search" value="공식">
                  <label class="border-checkbox-label" for="searchBtn2">공식</label>
                </div>
              </div>
              <div class="col-12 col-md-2 f-right col-sm-9 m-l-10 p-l-0 p-r-0 m-b-10 right-icon-control" style= "min-width: 242px;">
                <input type="text" class="form-control p-t-10 form-control-default search-text" value="<%= search %>" placeholder="제목검색" id="searchInput">
                <div class="form-icon" id="searchBtn">
                  <i class="fas fa-search"></i>
                </div>
              </div>
              <div class="col-md-2 p-r-0 p-l-0 f-right reportrange">
                <div id="reportrange" class="form-control form-control-default">
                  <i class="glyphicon glyphicon-calendar far fa-calendar-alt"></i>
                  <span></span>
                  <i class="f-right fa fa-angle-down" style="font-size: 17px;"></i>
                </div>
              </div>
            </div>
            <div class="card-block table-border-style">
              <div class="preloader3 loader-block">
                <div class="circ1 loader-primary"></div>
                <div class="circ2 loader-primary"></div>
                <div class="circ3 loader-primary"></div>
                <div class="circ4 loader-primary"></div>
              </div>
              <div class="table-responsive">
                <table class="table">
                  <thead>
                    <tr>
                      <th width="1%">
                        <div class="border-checkbox-section" style="max-width: 30px;">
                          <div class="border-checkbox-group border-checkbox-group-default">
                            <input class="border-checkbox" type="checkbox" id="checkboxAll">
                            <label class="border-checkbox-label" for="checkboxAll"></label>
                          </div>
                        </div>
                      </th>
                      <th width="5%">NO</th>
                      <th width="10%" style="min-width: 60px;">작성날짜</th>
                      <th width="20%" style="min-width:58px">제목</th>
                      <th width="5%" style="min-width: 80px;">언론사</th>
                      <th width="5%" style="min-width: 80px;">기자</th>
                      <th width="5%" style="min-width: 80px;">키워드</th>
                      <th width="5%" style="min-width: 50px;">분류글</th>
                      <th width="20%" style="min-width: 120px;">썸네일</th>
                      <th width="10%" style="min-width: 140px;">대분류</th>
                      <th width="10%" style="min-width: 100px;">대표기사</th>
                      <th width="10%" style="min-width: 100px;">세부기사</th>
                    </tr>
                  </thead>
                  <tbody>
                    <%
                      if(currentPage > 0){
                        (currentPage - 1) * limit;
                      }
                      for(var i=0; i < list.length; i++) {
                    %>
                    <tr>
                      <input type="hidden" id="idx" value="<%=list[i].media_idx%>" />
                      <td>
                        <div class="border-checkbox-section" style="max-width: 30px;">
                          <div class="border-checkbox-group border-checkbox-group-default">
                            <input class="border-checkbox news-checkbox" type="checkbox" id="checkbox<%=i%>">
                            <label class="border-checkbox-label" for="checkbox<%=i%>"></label>
                          </div>
                        </div>
                      </td>
                      <th><%= listCount - i - offset %></th>
                      <td><%= list[i].writeDate %></td>
                      <td>
                        <a href="<%= list[i].url %>" target="_blank" title="<%= list[i].media_title %>">
                          <div class="title_nobr"><%= list[i].media_title %></div>
                        </a>
                      </td>
                      <td><div class="nobr" title="<%= list[i].media_name %>"><%= list[i].media_name %></div></td>
                      <td>
                        <div class="nobr" title="<%= list[i].reporter_name %>">
                          <% if(list[i].reporter_name == null){ %>
                            <i class="fas fa-minus"></i>
                          <%}else{%>
                            <%=list[i].reporter_name%>
                          <%}%>
                        </div>
                      </td>
                      <td><div class="key_nobr" title="<%= list[i].keyword %>"><%= list[i].keyword %></div></td>
                      <td>
                        <div class="nobr" title="<%= list[i].textType %>">
                          <% if(list[i].textType == null){ %>
                            <i class="fas fa-minus"></i>
                          <%}else{%>
                            <%=list[i].textType%>
                          <%}%>
                        </div>
                      </td>
                      <td>
                        <input type="text" placeholder="이미지주소(URL)" id="thumbnail" class="form-control selectText">
                      </td>
                      <td>
                        <!-- <input type="number" min="1" max="8" value="1"  id="newsType" class="form-control"> -->
                        <select class="form-control form-control-default p-l-5 select-news" id="newsType">
                          <option value="0" <%=(list[i].type == '0') ? 'selected' : ''%>>-</option>
                          <option value="1" <%=(list[i].type == '1') ? 'selected' : (list[i].title_key == '쇼박스영화') ? 'selected' : ''%>>쇼박스영화</option>
                          <option value="2" <%=(list[i].type == '2') ? 'selected' : (list[i].title_key == '경쟁영화') ? 'selected' : ''%>>경쟁영화</option>
                          <option value="3" <%=(list[i].type == '3') ? 'selected' : (list[i].title_key == '캐스팅') ? 'selected' : ''%>>캐스팅</option>
                          <option value="4" <%=(list[i].type == '4') ? 'selected' : (list[i].title_key == '쇼박스기업뉴스' || list[i].title_key == '오리온') ? 'selected' : ''%>>쇼박스기업뉴스</option>
                          <option value="5" <%=(list[i].type == '5') ? 'selected' : (list[i].title_key == '영화일반') ? 'selected' : ''%>>영화일반</option>
                          <option value="6" <%=(list[i].type == '6') ? 'selected' : (list[i].title_key == '보도국') ? 'selected' : ''%>>보도국</option>
                          <option value="7" <%=(list[i].type == '7') ? 'selected' : (list[i].title_key == '박스오피스') ? 'selected' : ''%>>박스오피스</option>
                        </select>
                      </td>
                      <td>
                        <input type="text" placeholder="대표기사" id="news_title" value="<%=list[i].media_idx%>" class="form-control selectText">
                      </td>
                      <td>
                        <input type="text" placeholder="세부기사" id="news_detail" class="form-control selectText">
                      </td>
                    </tr>
                    <% } %>
                  </tbody>
                  <tfoot>
                    <tr>
                    <%
                    var pageCount = Math.ceil(listCount/limit);
                    if(pageCount > 1) {
                    %>
                      <td colspan="5">
                        <button id="addBtn" class="btn btn-list alert-check p-r-5 p-l-5 m-l-15 m-b-5  f-left m-t-5"><i class="fas fa-angle-down"></i>선택저장</button>
                      </td>
                      <td colspan="7">
                        <ul class="pagination float-right">
                          <%
                            var pageSize = 5;
                            var pRCnt = parseInt(currentPage / pageSize);
                            if(currentPage % pageSize == 0){
                              pRCnt = parseInt(currentPage / pageSize) - 1;
                            }
                            if(currentPage > 5) {
                          %>
                            <li class="page-item">
                              <a class="page-link" data-value="1" aria-label="Previous">
                                <i class="ti-angle-double-left f-12"></i>
                                <span aria-hidden="true"></span>
                                <span class="sr-only">Previous</span>
                              </a>
                            </li>
                            <li class="page-item">
                              <a class="page-link" data-value=="<%=(pRCnt * pageSize)%>" aria-label="Previous">
                                <i class="ti-angle-left f-12"></i>
                                <span aria-hidden="true"></span>
                                <span class="sr-only">Previous</span>
                              </a>
                            </li>
                          <% } %>
                          <% for(var index=pRCnt * pageSize + 1;index<(pRCnt + 1)*pageSize + 1;index++){%>
                            <li class= "<%=(currentPage == index) ? 'active' : ''%> page-item">
                              <a class="page-link" data-value="<%=index%>" >
                                <%= index %>
                              </a>
                            </li>
                          <%
                              if(index == pageCount) {
                                break;
                              }
                            }
                          %>
                          <% if((pRCnt + 1) * pageSize < pageCount) { %>
                            <li class="page-item">
                              <a class="page-link" data-value="<%=(pRCnt + 1)*pageSize+1%>" aria-label="Next">
                                <i class="ti-angle-right f-12"></i>
                                <span aria-hidden="true"></span>
                                <span class="sr-only">Next</span>
                              </a>
                            </li>
                            <li class="page-item">
                              <a class="page-link" data-value="<%=pageCount%>" aria-label="Next">
                                <i class="ti-angle-double-right f-12"></i>
                                <span aria-hidden="true"></span>
                                <span class="sr-only">Next</span>
                              </a>
                            </li>
                          <% } %>
                        </ul>
                      </td>
                      <% } else { %>
                        <td colspan="12">
                          <button id="addBtn" class="btn btn-list alert-check p-r-5 p-l-5 m-l-15 m-b-5  f-left m-t-5"><i class="fas fa-angle-down"></i>선택저장</button>
                        </td>
                      <% } %>
                    </tr>
                  </tfoot>
                </table>
                <input type="hidden" id="sDate" value="<%=sDate%>"/>
                <input type="hidden" id="eDate" value="<%=eDate%>"/>
                <input type="hidden" id="keyword" value="<%=keyword%>"/>
                <input type="hidden" id="search" value="<%=search%>"/>
                <input type="hidden" id="search2" value="<%=search2%>"/>
                <input type="hidden" id="limit" value="<%=limit%>"/>
                <input type="hidden" id="type" value="<%=type%>"/>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
<style type="text/css">
  .border-checkbox-section{
    min-width: 148px;
  }
  .border-checkbox-group-default{
    max-width:  58px;
    float:  left;
  }
  /* loader */
  .preloader3{
    display: none;
    padding-top:20%;
    position: fixed;
    left: 0;
    right: 0;
    top: 100px;
    width: 500px;
    margin: auto;
    max-width: 100%;
    z-index: 10000;
  }
  /* select box width */
  #searchInput {
    font-size: 12px;
  }
  #selectNewsType,#selectKeyword,#selectNum{
    min-width: 100px;
  }
  .reportrange{
    min-width: 207px;
  }
  .table{
    font-size: 12px;
  }
  .table td, .table th {
    text-align: center;
    vertical-align: middle;
  }
  .table input, .table select ,.table tbody div{
    font-size: 12px;
  }
  .title_nobr,.nobr,.key_nobr{
    font-size: 15px;
    padding-left: 3px;
    text-overflow: ellipsis;
    width: 400px;
    overflow: hidden;
  }
  .nobr{
    width: 60px;
  }
  .key_nobr{
    width: 102px;
  }
  .nobr>i,.key_nobr>i{
    font-size: 10px;
  }
  .select-news{
    font-size: 12px;
    max-height: 35px;
    line-height: 16px;
  }
  .title_nobr:hover{
    cursor:pointer;
    font-weight: bold;
  }
  .border-checkbox-section .border-checkbox-group .border-checkbox-label{
    margin: 0px;
  }
  #listTable .table thead th{
    vertical-align: middle;
    text-align: center;
  }
  #addBtn{
    font-weight: bold;
    color: white;
    height: 40px;
    background-color: #a6a6a6;
    border-color: #a6a6a6;
  }
  /* daterangepicker */
  .card .card-header span:not(.input-group-addon) {
    display: inline;
  }
</style>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-daterangepicker/2.1.27/daterangepicker.css" />
<script type="text/javascript" src="/js/error.js"></script>
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-daterangepicker/2.1.27/moment.js"></script>
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-daterangepicker/2.1.27/daterangepicker.js"></script>
<!-- sweetalert -->
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/sweetalert@2.1.0/dist/sweetalert.min.js"></script>
<script type="text/javascript">
  // data input hidden values
  var sDateValue = $('#sDate').val();
  var eDateValue = $('#eDate').val();
  var keywordValue = $('#keyword').val();
  var limitValue = $('#limit').val();
  var typeValue = $('#type').val();
  $(document).ready(function(){
    if(keywordValue != ''){
      $("#selectKeyword option[value='"+keywordValue+"']").attr('selected','selected');
    }
    if(typeValue != ''){
      $("#selectNewsType option[value='"+typeValue+"']").attr('selected','selected');
    }
    if(limitValue != ''){
      $("#selectNum option[value='"+limitValue+"']").attr('selected','selected');
    }
    $('#reportrange').daterangepicker(optionSet1,cb);

    if(sDateValue == '' && eDateValue == ''){
      $('#reportrange span').html(moment().format('YYYY.MM.DD') + ' - ' + moment().format('YYYY.MM.DD'));
    }
    else{
      $('#reportrange span').html(moment(new Date(sDateValue)).format('YYYY.MM.DD') + ' - ' + moment(new Date(eDateValue+' 23:59:59')).format('YYYY.MM.DD'));
    }
  });
  // 라디오버튼처럼 checkvox name값 설정
  $('input[type="checkbox"][name="search"]').on('click',function(){
    //click 이벤트가 발생했는지 체크
    if($(this).prop('checked')){
      // checkbox전체를 checked 해제후 click한 요소만 true지정
      $('input[type="checkbox"][name="search"]').prop('checked',false);
      $(this).prop('checked',true);
      $('#search2').val($(this).val());
       searchStrBtn($(this).val());
    }
    else{
      $('#search2').val('');
      searchStrBtn('');
    }
  });
  // 단독, 공식 버튼 클릭시
  function searchStrBtn(search){
    var param = {
      page : 1
    };
    if(search != ''){
      param.search_b = search;
    }
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    if(sDateValue != '' && eDateValue != ''){
      param.sDate = sDateValue;
      param.eDate = eDateValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var keywordValue = $('#keyword').val();
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    ajaxGetPageList(param,0);
  }
  // 검색시
  $('#searchInput').on('keyup',function(event){
    if(event.keyCode == 13){
      $('#searchBtn').trigger('click');
    };
  });
  $('#searchBtn').on('click',function(){
    // var url = '/news?page=1';
    // var search = $("#searchInput").val();
    // if(search != ''){
    //   url += '&search='+$.urlPostParam(search);
    // }
    // var limitValue = $('#limit').val();
    // if(limitValue != ''){
    //   url += '&limit='+$.urlPostParam(limitValue);
    // }
    // location.href = url;
    var param = {
      page : 1
    };
    var inputVal = $("#searchInput").val();
    if(inputVal != ''){
      param.search=inputVal;
      $('#search').val(inputVal);
    }
    else{
      $('#search').val('');
    }
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    if(sDateValue != '' && eDateValue != ''){
      param.sDate = sDateValue;
      param.eDate = eDateValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var keywordValue = $('#keyword').val();
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    ajaxGetPageList(param,0);
  });
  // 대표기사,썸네일,세부기사 클릭시 텍스트 전체 선택
  $(document).on("click",".selectText", function(){
    $(this).select();
  });
  // 키워드 선택시
  $('#selectKeyword').on('change',function(){
    var param = {
      page : 1
    };
    var optionVal = $("#selectKeyword option:selected").val();
    if(optionVal != '키워드'){
      param.keyword=optionVal;
      $('#keyword').val(optionVal);
    }
    else{
      $('#keyword').val('');
    }
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    if(sDateValue != '' && eDateValue != ''){
      param.sDate = sDateValue;
      param.eDate = eDateValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    ajaxGetPageList(param,0);
  });
  // 페이지 행 선택시
  $('#selectNum').on('change',function(){
    var param = {
      page : 1
    };
    var optionVal = $("#selectNum option:selected").val();
    $('#limit').val(optionVal);
    param.limit = optionVal;
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    var keywordValue = $('#keyword').val();
    if(sDateValue != '' && eDateValue != ''){
      param.sDate = sDateValue;
      param.eDate = eDateValue;
    }
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    ajaxGetPageList(param,0);
  });
  // 뉴스 대분류 선택시
  $('#selectNewsType').on('change',function(){
    var param = {
      page : 1
    };
    var optionVal = $("#selectNewsType option:selected").val();
    if(optionVal != '0'){
      param.type=optionVal;
      $('#type').val(optionVal);
    }
    else{
      $('#type').val('');
    }
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    var keywordValue = $('#keyword').val();
    if(sDateValue != '' && eDateValue != ''){
      param.sDate = sDateValue;
      param.eDate = eDateValue;
    }
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    ajaxGetPageList(param,0);
  });
  // 뉴스 전체 선택시
  $('#checkboxAll').on('click',function(){
    var checkBool = null;
    if($(this).is(":checked")){
      checkBool = true;
    }else{
      checkBool = false;
    }
    $('.news-checkbox').prop('checked', checkBool);
  });
  $(document).on('click','.news-checkbox',function(){
    $('#checkboxAll').prop('checked', false);
  });
  // 선택저장 클릭시
  $(document).on('click','#addBtn',function(){
    if($('input.news-checkbox[type=checkbox]:checked').length < 1){
      swal("ERROR!", '체크박스를 선택해주세요.', "error");
      return false;
    }
    swal({
      title: "선택저장 처리 하시겠습니까?",
      icon: "warning",
      buttons: ["취소", true],
      dangerMode: true,
    }).then(function(value) {
      if (value != null) {
        var list = [];
        $('input.news-checkbox[type=checkbox]:checked').each(function(idx,ele){
          var trEle = $(ele).parents('tr');
          list.push({idx:trEle.find('#idx').val(),thumbnail:trEle.find('#thumbnail').val(),code:trEle.find('#newsType option:selected').val(),detail:trEle.find('#news_detail').val()});
        });
        $('#checkboxAll').prop('checked', false);
        addNews(list);
      }
    });
  });
  function addNews(arr){
    $('input[type=checkbox]:checked').prop('checked',false);
    $.ajax({
      type: "POST",
      url: "/news/addNews",
      data: {'list' : JSON.stringify(arr)},
      error:function(request,status,error){
        console.log('code:'+request.status+'\n'+'message:'+request.responseText+'\n'+'error:'+error);
        swal("ERROR!", '새로고침 후 다시 시도해주세요.', "error");
      },
      success:function(data){
        if(data.status){
          swal("SUCCESS!", '선택저장 완료했습니다.', "success");
        }else{
          swal("ERROR!", '새로고침 후 다시 시도해주세요.', "error");
        }
      }
    });
  }
  //페이지 이동
  $(document).on('click','.page-link',function(){
    var num = Number($(this).text()) || Number($(this).data().num);
    var param = {
      page : Number($(this).data().value)
    };
    // data input hidden values
    var sDateValue = $('#sDate').val();
    var eDateValue = $('#eDate').val();
    if(sDateValue != '' && eDateValue != '' ){
      param.sDate = moment(sDateValue).format('YYYY-MM-DD');
      param.eDate = moment(eDateValue).format('YYYY-MM-DD');
    }
    var keywordValue = $('#keyword').val();
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    ajaxGetPageList(param,num);
  });
  function ajaxGetPageList(param){
    var search2Value = $('#search2').val();
    if(search2Value != ''){
      param.search_b = search2Value;
    }
    console.log('ajaxGetPageList:',param);
    $('.preloader3').show();
    $.ajax({
      url: '/news/getNextPage',
      type: 'post',
      data: param,
      error: function(request,status,error){
        console.log('code:'+request.status+'\n'+'message:'+request.responseText+'\n'+'error:'+error);
        alert('새로고침 후 다시 시도해주세요.');
      },
      success: function(data){
        if(data.status != true){
          alert('새로고침 후 다시 시도해주세요.');
          return false;
        }
        if(typeof history.pushState == 'function'){
          var renewURL = location.href;//현재 주소를 가져온다.
          renewURL = renewURL.replace(/\?page=([0-9]+)/ig,'?page='+data.result.currentPage);//현재 주소 중 page 부분이 있다면 새로 부여될 페이지 번호를 할당한다.
          if(renewURL.indexOf("?page=") == -1){
            renewURL += '?page='+data.result.currentPage;
          }
          renewURL = renewURL.replace(/\&limit=([0-9]+)/ig,'&limit='+data.result.limit);//현재 주소 중 page 부분이 있다면 새로 부여될 페이지 번호를 할당한다.
          if(renewURL.indexOf("&limit=") == -1){
            renewURL += '&limit='+data.result.limit;
          }
          //페이지 갱신 실행!
          history.pushState(null, null,renewURL);
        }
        $('#listTable tbody').eq(0).empty();

        console.log(data);
        data.result.list.forEach(function(item,idx){
          var numIdx = Math.ceil(data.result.listCount-(data.result.offset+idx)).toString();
          var html = '<tr>\
          <input type="hidden" id="idx" value="'+item.media_idx+'" />\
          <td>\
            <div class="border-checkbox-section" style="max-width: 30px;">\
              <div class="border-checkbox-group border-checkbox-group-default">\
                <input class="border-checkbox news-checkbox" type="checkbox" id="checkbox'+idx+'">\
                <label class="border-checkbox-label" for="checkbox'+idx+'"></label>\
              </div>\
            </div>\
          </td>\
          <th>'+numIdx+'</th>\
          <td>'+item.writeDate+'</td>\
          <td>\
            <a href="'+item.url+'" target="_blank" title="'+item.media_title+'">\
              <div class="title_nobr">'+item.media_title+'</div>\
            </a>\
          </td>\
          <td><div class="nobr" title="'+item.media_name+'">'+item.media_name+'</div></td>\
          <td><div class="nobr" title="'+item.reporter_name+'">'+((item.reporter_name == null) ? '<i class="fas fa-minus"></i>' : item.reporter_name)+'</div></td>\
          <td><div class="key_nobr" title="'+item.keyword+'">'+item.keyword+'</div></td>\
          <td><div class="nobr" title="'+item.textType+'">'+((item.textType == null) ? '<i class="fas fa-minus"></i>' : item.textType)+'</div></td>\
          <td>\
            <input type="text" placeholder="이미지주소(URL)" id="thumbnail" class="form-control selectText">\
          </td>\
          <td>\
            <select class="form-control form-control-default p-l-5 select-news" id="newsType">\
              <option value="0" '+((item.type == '0') ? 'selected' : '')+'>-</option>\
              <option value="1" '+((item.type == '1') ? 'selected' : (item.title_key == '쇼박스영화') ? 'selected' : '')+'>쇼박스영화</option>\
              <option value="2" '+((item.type == '2') ? 'selected' : (item.title_key == '경쟁영화') ? 'selected' : '')+'>경쟁영화</option>\
              <option value="3" '+((item.type == '3') ? 'selected' : (item.title_key == '캐스팅') ? 'selected' : '')+'>캐스팅</option>\
              <option value="4" '+((item.type == '4') ? 'selected' : (item.title_key == '쇼박스기업뉴스' || item.title_key == '오리온') ? 'selected' : '')+'>쇼박스기업뉴스</option>\
              <option value="5" '+((item.type == '5') ? 'selected' : (item.title_key == '영화일반') ? 'selected' : '')+'>영화일반</option>\
              <option value="6" '+((item.type == '6') ? 'selected' : (item.title_key == '보도국') ? 'selected' : '')+'>보도국</option>\
              <option value="7" '+((item.type == '7') ? 'selected' : (item.title_key == '박스오피스') ? 'selected' : '')+'>박스오피스</option>\
            </select>\
          </td>\
          <td>\
            <input type="text" placeholder="대표기사" id="news_title" value="'+item.media_idx+'" class="form-control selectText">\
          </td>\
          <td>\
            <input type="text" placeholder="세부기사" id="news_detail" class="form-control selectText">\
          </td>';
          $('#listTable tbody').eq(0).append(html);
        });
        var limit = $('#limit').val();
        var pageCount = Math.ceil(data.result.listCount/limit);
        $('#listTable tfoot').eq(0).empty();
        var html = '<tr>';
        if(pageCount > 1) {
          html += '<td colspan="5"><button id="addBtn" class="btn btn-list alert-check p-r-5 p-l-5 m-l-15 m-b-5  f-left m-t-5"><i class="fas fa-angle-down"></i>선택저장</button></td>';
          html += '<td colspan="7"><ul class="pagination float-right">';
          var pageSize = 5;
          var pRCnt = parseInt(data.result.currentPage / pageSize);
          if(data.result.currentPage % pageSize == 0){
            pRCnt = parseInt(data.result.currentPage / pageSize) - 1;
          }
          if(data.result.currentPage > 5) {
            html += '<li class="page-item"><a class="page-link" data-value="1" aria-label="Previous">\
                <i class="ti-angle-double-left f-12"></i>\
                <span aria-hidden="true"></span>\
                <span class="sr-only">Previous</span>\
              </a></li>\
              <li class="page-item"><a class="page-link" data-value=\"'+(pRCnt * pageSize)+'"\ aria-label="Previous">\
                <i class="ti-angle-left f-12"></i>\
                <span aria-hidden="true"></span>\
                <span class="sr-only">Previous</span>\
              </a></li>';
          }

          for(var index=pRCnt * pageSize + 1;index<(pRCnt + 1)*pageSize + 1;index++){
            var active = (data.result.currentPage == index) ? "active" : "";
            html += '<li class=\"'+active+' page-item">\
              <a class="page-link" data-value=\"'+index+'"\ >'+index+'</a></li>'

            if(index == pageCount) {
              break;
            }
          }
          if((pRCnt + 1) * pageSize < pageCount) {
            html += '<li class="page-item">\
                  <a class="page-link" data-value=\"'+((pRCnt + 1)*pageSize+1)+'"\ aria-label="Next">\
                    <i class="ti-angle-right f-12"></i>\
                    <span aria-hidden="true"></span>\
                    <span class="sr-only">Next</span>\
                  </a></li>\
                  <li class="page-item">\
                  <a class="page-link" data-value=\"'+pageCount+'"\ aria-label="Next">\
                    <i class="ti-angle-double-right f-12"></i>\
                    <span aria-hidden="true"></span>\
                    <span class="sr-only">Next</span>\
                  </a></li>';
          }
          html += '</ul></td>';
        }
        else{
          html += '<td colspan="12"><button id="addBtn" class="btn btn-list alert-check p-r-5 p-l-5 m-l-15 m-b-5  f-left m-t-5"><i class="fas fa-angle-down"></i>선택저장</button></td>';
        }
        html += '</tr>';
        $('#listTable tfoot').eq(0).append(html);
        $('.preloader3').fadeOut(500);
      }
    });
  }

  //날짜 선택 시
  function searchDate(){
    var dateArr = $("#reportrange span").text().split(' - ');
    var start = moment(new Date(dateArr[0].replace(/[.]/gi,'-')));
    var end = moment(new Date(dateArr[1].replace(/[.]/gi,'-')));
    var param = {
      page : 1,
      sDate : start.format('YYYY-MM-DD'),
      eDate : end.format('YYYY-MM-DD')
    };
    $('#sDate').val(param.sDate);
    $('#eDate').val(param.eDate);

    // data input hidden values
    var keywordValue = $('#keyword').val();
    if(keywordValue != ''){
      param.keyword = keywordValue;
    }
    var typeValue = $('#type').val();
    if(typeValue != ''){
      param.type = typeValue;
    }
    var searchValue = $('#searchInput').val();
    if(searchValue != ''){
      param.search = searchValue;
    }
    var limitValue = $('#limit').val();
    if(limitValue != ''){
      param.limit = limitValue;
    }
    ajaxGetPageList(param);
  }
  $(document).on('click','.applyBtn',searchDate);
  //날짜 설정
  var cb = function(start, end, label) {
    console.log('FUNC: cb',start.toISOString(), end.toISOString(), label);
    $('#reportrange span').html(moment(new Date(start.toISOString())).format('YYYY.MM.DD') + ' - ' + moment(new Date(end.toISOString())).format('YYYY.MM.DD'));
    searchDate();
  }
  var ranges = {
    '당일': [moment(), moment()],
    '전일': [moment().subtract(1, 'days'), moment()],
    '최근 7일': [moment().subtract(6, 'days'), moment()],
    '최근 30일': [moment().subtract(29, 'days'), moment()]
  };
  var optionSet1 = {
    startDate: (sDateValue != '') ? moment(new Date(sDateValue)) : moment(),
    endDate:  (eDateValue != '') ? moment(new Date(eDateValue+' 23:59:59')) : moment(),
    dateLimit: {
      days: 60
    },
    showDropdowns: true,
    showRangeInputsOnCustomRangeOnly: false,
    ranges: ranges,
    opens: 'left',
    buttonClasses: ['btn'],
    applyClass: 'btn-small btn-primary',
    cancelClass: 'btn-small',
    format: 'YYYY.MM.DD',
    separator: ' to ',
    locale: {
      applyLabel: '확인',
      cancelLabel: '취소',
      customRangeLabel: '직접선택',
      daysOfWeek: ['일', '월', '화', '수', '목', '금', '토'],
      monthNames: ['1월', '2월', '3월', '4월', '5월', '6월', '7월', '8월', '9월', '10월', '11월', '12월'],
      firstDay: 1
    }
  };
</script>
