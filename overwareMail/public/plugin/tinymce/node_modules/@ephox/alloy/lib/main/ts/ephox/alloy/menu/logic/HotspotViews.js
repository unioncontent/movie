import { Option } from '@ephox/katamari';
import { Focusing } from '../../api/behaviour/Focusing';
import { Sandboxing } from '../../api/behaviour/Sandboxing';
var onEscape = function (anchor, sandbox) {
    Sandboxing.closeSandbox(sandbox);
    // TODO: Move.
    if (anchor.anchor === 'hotspot') {
        Focusing.focus(anchor.hotspot);
    }
    else if (anchor.anchor === 'makeshift') {
        anchor.onEscape(sandbox);
    }
    return Option.some(true);
};
export default {
    onEscape: onEscape
};
//# sourceMappingURL=HotspotViews.js.map