import { FieldSchema, ValueSchema } from '@ephox/boulder';
import { Fun } from '@ephox/katamari';
var menuFields = Fun.constant([
    FieldSchema.strict('menu'),
    FieldSchema.strict('selectedMenu')
]);
var itemFields = Fun.constant([
    FieldSchema.strict('item'),
    FieldSchema.strict('selectedItem')
]);
var schema = Fun.constant(ValueSchema.objOfOnly(itemFields().concat(menuFields())));
var itemSchema = Fun.constant(ValueSchema.objOfOnly(itemFields()));
export { menuFields, itemFields, schema, itemSchema };
//# sourceMappingURL=MenuMarkers.js.map