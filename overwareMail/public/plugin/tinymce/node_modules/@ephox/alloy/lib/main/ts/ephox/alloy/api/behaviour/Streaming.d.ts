import * as Behaviour from './Behaviour';
import { AlloyComponent } from '../../api/component/ComponentApi';
import { SimulatedEvent } from '../../events/SimulatedEvent';
export interface StreamingBehaviour extends Behaviour.AlloyBehaviour {
    config: (config: StreamingConfig) => {
        [key: string]: (any) => any;
    };
}
export interface StreamingConfig {
    stream: {
        mode: StreamMode;
        delay: number;
    };
    event?: any;
    onStream: (component: AlloyComponent, simulatedEvent: SimulatedEvent) => void;
}
export declare type StreamMode = 'throttle';
declare const Streaming: StreamingBehaviour;
export { Streaming };
