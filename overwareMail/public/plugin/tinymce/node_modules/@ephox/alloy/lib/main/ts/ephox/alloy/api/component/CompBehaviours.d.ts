import { SketchSpec } from '../../api/ui/Sketcher';
import { AlloyBehaviour } from '../../api/behaviour/Behaviour';
export interface ComponentBehaviour {
    data: {
        key: string;
        value: () => AlloyBehaviour;
    };
    list: AlloyBehaviour[];
}
declare const generateFrom: (spec: SketchSpec, all: AlloyBehaviour[]) => ComponentBehaviour;
declare const generate: (spec: any) => ComponentBehaviour;
export { generate, generateFrom };
