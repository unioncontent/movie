import * as Gui from './Gui';
import { SugarElement, SugarEvent } from '../../alien/TypeDefinitions';
import { AlloyComponent } from '../../api/component/ComponentApi';
export interface ForeignGuiSpec {
    root: () => SugarElement;
    dispatchers: any[];
    insertion?: (root: AlloyComponent, system: Gui.GuiSystem) => void;
}
declare const engage: (spec: ForeignGuiSpec) => {
    dispatchTo: (type: string, event: SugarEvent) => void;
    unproxy: (component: AlloyComponent) => void;
    disengage: () => void;
};
export { engage };
