import { SketchSpec } from '../api/ui/Sketcher';
export default function (): {
    getEvents: (elem: any, spec: SketchSpec) => {
        elem: (...x: any[]) => any;
        evts: (...x: any[]) => any;
    };
};
