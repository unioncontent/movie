export default class BinaryReader {
    protected _dv: DataView;
    littleEndian: boolean;
    constructor(ar: ArrayBuffer);
    readByteAt(idx: any): number;
    read(idx: any, size: any): number;
    BYTE(idx: any): number;
    SHORT(idx: any): number;
    LONG(idx: any): number;
    SLONG(idx: any): number;
    CHAR(idx: any): string;
    STRING(idx: any, count: any): string;
    SEGMENT(idx: any, size: any): ArrayBuffer;
    asArray(type: any, idx: any, count: any): any[];
    length(): number;
}
