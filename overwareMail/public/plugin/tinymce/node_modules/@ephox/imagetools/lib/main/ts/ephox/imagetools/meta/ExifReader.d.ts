import BinaryReader from './BinaryReader';
export default class ExifReader extends BinaryReader {
    private _idx;
    private _offsets;
    private _tiffTags;
    constructor(ar: ArrayBuffer);
    UNDEFINED(): any;
    RATIONAL(idx: any): number;
    SRATIONAL(idx: any): number;
    ASCII(idx: any): string;
    TIFF(): any;
    EXIF(): any;
    GPS(): any;
    thumb(): ArrayBuffer;
    private extractTags(IFD_offset, tags2extract);
}
