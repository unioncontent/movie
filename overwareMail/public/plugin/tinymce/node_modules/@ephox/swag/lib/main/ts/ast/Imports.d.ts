import * as estree from 'estree';
interface ImportInfo {
    kind: string;
    name: string;
    fromName: string;
    modulePath: string;
}
declare const createImport: (kind: string, name: string, fromName: string, modulePath: string) => ImportInfo;
declare const readImports: (program: estree.Program) => ImportInfo[];
declare const toAst: (imports: ImportInfo[]) => estree.ImportDeclaration[];
export { ImportInfo, readImports, createImport, toAst };
