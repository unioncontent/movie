import { FileSystem } from '../fs/FileSystem';
interface Options {
    fileSystem?: FileSystem;
    forceFlat: boolean;
}
declare const remapImports: (options?: Options) => {
    name: string;
    transform: (code: string, id: string) => any;
};
export { Options, remapImports };
