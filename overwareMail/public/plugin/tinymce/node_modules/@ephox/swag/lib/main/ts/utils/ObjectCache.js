"use strict";
exports.__esModule = true;
var createObjectCache = function () {
    var cache = {};
    var get = function (key) { return cache[key]; };
    var put = function (key, value) { return cache[key] = value; };
    var getOrThunk = function (key, f) {
        var item = get(key);
        if (item === undefined) {
            var newItem = f();
            put(key, newItem);
            return newItem;
        }
        else {
            return item;
        }
    };
    return {
        get: get,
        put: put,
        getOrThunk: getOrThunk
    };
};
exports.createObjectCache = createObjectCache;
