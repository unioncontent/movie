import { Prefixes } from '../fs/Resolve';
import { FileSystem } from '../fs/FileSystem';
interface Options {
    basedir: string;
    prefixes: Prefixes;
    forceFlat: boolean;
    fileSystem?: FileSystem;
}
declare const nodeResolve: (options?: Options) => {
    name: string;
    resolveId: (importee: string, importer: string) => Promise<string>;
};
export { Options, nodeResolve };
